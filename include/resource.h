#ifndef POKEBLACK_RESOURCE_H
#define POKEBLACK_RESOURCE_H
/*
 * TOMBSTONE. The unverified Ghidra "ResourceObject"/"ResourceData" layouts
 * that used to live here moved to "ghidra_legacy.h". They were never checked
 * against the ROM and their addresses were on the legacy +0x02000000 base.
 * New code should not use them; see include/README.md.
 */
#include "ghidra_legacy.h"
#endif // POKEBLACK_RESOURCE_H
